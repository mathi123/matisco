<UserControl x:Class="Example.BusinessApp.Infrastructure.Views.StartUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mvvm="http://prismlibrary.com/"
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <Button Content="Open customers in new window" Width="100" Height="25" Command="{Binding OpenCustomersCommand}"/>

        <Button Content="Test modals" Width="100" Height="25" Command="{Binding OpenModalSamplesCommand}"/>

        <Button Content="Exit application" Width="100" Height="25" Command="{Binding ExitApplicationCommand}" />
    </StackPanel>
</UserControl>
