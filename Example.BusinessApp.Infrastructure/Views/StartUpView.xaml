<UserControl x:Class="Example.BusinessApp.Infrastructure.Views.StartUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mvvm="http://prismlibrary.com/"
             xmlns:mab="clr-namespace:Matisco.Wpf.Controls.Buttons;assembly=Matisco.Wpf.Controls"
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <mab:Button Text="Open customers in new window" Command="{Binding OpenCustomersCommand}"/>

        <mab:Button Text="Test modals" Command="{Binding OpenModalSamplesCommand}"/>

        <mab:Button Text="Exit application" Command="{Binding ExitApplicationCommand}" />
    </StackPanel>
</UserControl>
